
TString values = "mod1.sh";
 // Check that the file exists and open it.
  ifstream mod1;
  mod1.open();
  if(!mod1.good()) {
    cout << "File Error or File Missing" <<endl; 
    mod1.close(); 
    return -1;
  }// if(!mod1.good())



                               // VCAL 10
  /* /////////////////////////////////////////////////////////////////////////////////////
   * The following code makes an array of ints that holds that data we want to examine. 
   *////////////////////////////////////////////////////////////////////////////////////
  // Define a string that will hold each line of data in the file.
  TString aLine;
  // Define next that will hold the current token value in the tokenized string.
  char *next;
  //   lines = # of line in the data file, x = # of tokens(trimbits, pix, col, rows) in each line, 
  int x,lines = 0;//y,z,
  int data10[16][2];
  // Loop over the data file to pick the data we want to plot
  while(aLine.ReadLine(mod1)) {
    // char pointer that points to data line
    char* tmpstr = aLine;
    // next holds the current token value in the tokenized string
    next = strtok (tmpstr, splitter);
    // Loop over the tokens in the data line
    while (next != NULL){

	// Fill in the array with the data. This is the data we will plot!!
      data10[lines][x] = atof(next);

      next = strtok (0, splitter);
      x++;
    }// while(next != Null)
    x = 0;

    lines++;
  }


                                 //VCAL 15
  /* /////////////////////////////////////////////////////////////////////////////////////
   * The following code makes an array of ints that holds that data we want to examine. 
   *////////////////////////////////////////////////////////////////////////////////////
  // Define a string that will hold each line of data in the file.
  TString aLine;
  // Define next that will hold the current token value in the tokenized string.
  char *next;
  //   lines = # of line in the data file, x = # of tokens(trimbits, pix, col, rows) in each line, 
  int x,lines = 0;//y,z,
  int data15[16][2];
  // Loop over the data file to pick the data we want to plot
  while(aLine.ReadLine(mod1)) {
    // char pointer that points to data line
    char* tmpstr = aLine;
    // next holds the current token value in the tokenized string
    next = strtok (tmpstr, splitter);
    // Loop over the tokens in the data line
    while (next != NULL){

	// Fill in the array with the data. This is the data we will plot!!
      data15[lines][x] = atof(next);

      next = strtok (0, splitter);
      x++;
    }// while(next != Null)
    x = 0;

    lines++;
  }




                                //VCAL 20
  /* /////////////////////////////////////////////////////////////////////////////////////
   * The following code makes an array of ints that holds that data we want to examine. 
   *////////////////////////////////////////////////////////////////////////////////////
  // Define a string that will hold each line of data in the file.
  TString aLine;
  // Define next that will hold the current token value in the tokenized string.
  char *next;
  //   lines = # of line in the data file, x = # of tokens(trimbits, pix, col, rows) in each line, 
  int x,lines = 0;//y,z,
  int data20[16][2];
  // Loop over the data file to pick the data we want to plot
  while(aLine.ReadLine(mod1)) {
    // char pointer that points to data line
    char* tmpstr = aLine;
    // next holds the current token value in the tokenized string
    next = strtok (tmpstr, splitter);
    // Loop over the tokens in the data line
    while (next != NULL){

	// Fill in the array with the data. This is the data we will plot!!
      data20[lines][x] = atof(next);

      next = strtok (0, splitter);
      x++;
    }// while(next != Null)
    x = 0;

    lines++;
  }



                               //VCAL 25
  /* /////////////////////////////////////////////////////////////////////////////////////
   * The following code makes an array of ints that holds that data we want to examine. 
   *////////////////////////////////////////////////////////////////////////////////////
  // Define a string that will hold each line of data in the file.
  TString aLine;
  // Define next that will hold the current token value in the tokenized string.
  char *next;
  //   lines = # of line in the data file, x = # of tokens(trimbits, pix, col, rows) in each line, 
  int x,lines = 0;//y,z,
  int data25[16][2];
  // Loop over the data file to pick the data we want to plot
  while(aLine.ReadLine(mod1)) {
    // char pointer that points to data line
    char* tmpstr = aLine;
    // next holds the current token value in the tokenized string
    next = strtok (tmpstr, splitter);
    // Loop over the tokens in the data line
    while (next != NULL){

	// Fill in the array with the data. This is the data we will plot!!
      data25[lines][x] = atof(next);

      next = strtok (0, splitter);
      x++;
    }// while(next != Null)
    x = 0;

    lines++;
  }




                               //VCAL 30
  /* /////////////////////////////////////////////////////////////////////////////////////
   * The following code makes an array of ints that holds that data we want to examine. 
   *////////////////////////////////////////////////////////////////////////////////////
  // Define a string that will hold each line of data in the file.
  TString aLine;
  // Define next that will hold the current token value in the tokenized string.
  char *next;
  //   lines = # of line in the data file, x = # of tokens(trimbits, pix, col, rows) in each line, 
  int x,lines = 0;//y,z,
  int data30[16][2];
  // Loop over the data file to pick the data we want to plot
  while(aLine.ReadLine(mod1)) {
    // char pointer that points to data line
    char* tmpstr = aLine;
    // next holds the current token value in the tokenized string
    next = strtok (tmpstr, splitter);
    // Loop over the tokens in the data line
    while (next != NULL){

	// Fill in the array with the data. This is the data we will plot!!
      data30[lines][x] = atof(next);

      next = strtok (0, splitter);
      x++;
    }// while(next != Null)
    x = 0;

    lines++;
  }

                               //VCAL 35
  /* /////////////////////////////////////////////////////////////////////////////////////
   * The following code makes an array of ints that holds that data we want to examine. 
   *////////////////////////////////////////////////////////////////////////////////////
  // Define a string that will hold each line of data in the file.
  TString aLine;
  // Define next that will hold the current token value in the tokenized string.
  char *next;
  //   lines = # of line in the data file, x = # of tokens(trimbits, pix, col, rows) in each line, 
  int x,lines = 0;//y,z,
  int data35[16][2];
  // Loop over the data file to pick the data we want to plot
  while(aLine.ReadLine(mod1)) {
    // char pointer that points to data line
    char* tmpstr = aLine;
    // next holds the current token value in the tokenized string
    next = strtok (tmpstr, splitter);
    // Loop over the tokens in the data line
    while (next != NULL){

	// Fill in the array with the data. This is the data we will plot!!
      data35[lines][x] = atof(next);

      next = strtok (0, splitter);
      x++;
    }// while(next != Null)
    x = 0;

    lines++;
  }


                               //VCAL 40
  /* /////////////////////////////////////////////////////////////////////////////////////
   * The following code makes an array of ints that holds that data we want to examine. 
   *////////////////////////////////////////////////////////////////////////////////////
  // Define a string that will hold each line of data in the file.
  TString aLine;
  // Define next that will hold the current token value in the tokenized string.
  char *next;
  //   lines = # of line in the data file, x = # of tokens(trimbits, pix, col, rows) in each line, 
  int x,lines = 0;//y,z,
  int data40[16][2];
  // Loop over the data file to pick the data we want to plot
  while(aLine.ReadLine(mod1)) {
    // char pointer that points to data line
    char* tmpstr = aLine;
    // next holds the current token value in the tokenized string
    next = strtok (tmpstr, splitter);
    // Loop over the tokens in the data line
    while (next != NULL){

	// Fill in the array with the data. This is the data we will plot!!
      data40[lines][x] = atof(next);

      next = strtok (0, splitter);
      x++;
    }// while(next != Null)
    x = 0;

    lines++;
  }


                               // VCAL 45
  /* /////////////////////////////////////////////////////////////////////////////////////
   * The following code makes an array of ints that holds that data we want to examine. 
   *////////////////////////////////////////////////////////////////////////////////////
  // Define a string that will hold each line of data in the file.
  TString aLine;
  // Define next that will hold the current token value in the tokenized string.
  char *next;
  //   lines = # of line in the data file, x = # of tokens(trimbits, pix, col, rows) in each line, 
  int x,lines = 0;//y,z,
  int data45[16][2];
  // Loop over the data file to pick the data we want to plot
  while(aLine.ReadLine(mod1)) {
    // char pointer that points to data line
    char* tmpstr = aLine;
    // next holds the current token value in the tokenized string
    next = strtok (tmpstr, splitter);
    // Loop over the tokens in the data line
    while (next != NULL){

	// Fill in the array with the data. This is the data we will plot!!
      data45[lines][x] = atof(next);

      next = strtok (0, splitter);
      x++;
    }// while(next != Null)
    x = 0;

    lines++;
  }






                                //VCAL 55
  /* /////////////////////////////////////////////////////////////////////////////////////
   * The following code makes an array of ints that holds that data we want to examine. 
   *////////////////////////////////////////////////////////////////////////////////////
  // Define a string that will hold each line of data in the file.
  TString aLine;
  // Define next that will hold the current token value in the tokenized string.
  char *next;
  //   lines = # of line in the data file, x = # of tokens(trimbits, pix, col, rows) in each line, 
  int x,lines = 0;//y,z,
  int data55[16][2];
  // Loop over the data file to pick the data we want to plot
  while(aLine.ReadLine(mod1)) {
    // char pointer that points to data line
    char* tmpstr = aLine;
    // next holds the current token value in the tokenized string
    next = strtok (tmpstr, splitter);
    // Loop over the tokens in the data line
    while (next != NULL){

	// Fill in the array with the data. This is the data we will plot!!
      data55[lines][x] = atof(next);

      next = strtok (0, splitter);
      x++;
    }// while(next != Null)
    x = 0;

    lines++;
  }



                               //VCAL 60
  /* /////////////////////////////////////////////////////////////////////////////////////
   * The following code makes an array of ints that holds that data we want to examine. 
   *////////////////////////////////////////////////////////////////////////////////////
  // Define a string that will hold each line of data in the file.
  TString aLine;
  // Define next that will hold the current token value in the tokenized string.
  char *next;
  //   lines = # of line in the data file, x = # of tokens(trimbits, pix, col, rows) in each line, 
  int x,lines = 0;//y,z,
  int data60[16][2];
  // Loop over the data file to pick the data we want to plot
  while(aLine.ReadLine(mod1)) {
    // char pointer that points to data line
    char* tmpstr = aLine;
    // next holds the current token value in the tokenized string
    next = strtok (tmpstr, splitter);
    // Loop over the tokens in the data line
    while (next != NULL){

	// Fill in the array with the data. This is the data we will plot!!
      data60[lines][x] = atof(next);

      next = strtok (0, splitter);
      x++;
    }// while(next != Null)
    x = 0;

    lines++;
  }


 //defining a canvas to hold our graph
  c1_trim = new TCanvas("c1_trim","",200,10,800,600);
  //dfining a multigraph to hold different pixels
  // TMultiGraph *mg = new TMultiGraph();
  //TGraph *gr[20];
  TLegend *leg1 = new TLegend(0.910053,0.25,0.984127,0.900442);
  leg1->SetFillColor(0);


  int trimVals[] = {10,15,20,25,30,35,40,45,55,60};


int trimbits1[] = {data10[0][0],data10[1][0], data10[2][0],data10[3][0], data10[4][0],data10[5][0], data10[6][0],data10[7][0],data10[8][0],data10[9][0],data10[10][0],data10[11][0],data10[12][0],data10[13][0],data10[14][0],data10[15][0]};

  int trimbits2[] = {data15[0][0],data15[1][0], data15[2][0],data15[3][0], data15[4][0],data15[5][0], data15[6][0],data15[7][0],data15[8][0],data15[9][0],data15[10][0],data15[11][0],data15[12][0],data15[13][0],data15[14][0],data15[15][0]};

  //trimbits values for pixel 1500(second plot data)

  int trimbits3[] =  {data20[0][0],data20[1][0], data20[2][0],data20[3][0], data20[4][0],data20[5][0], data20[6][0],data20[7][0],data20[8][0],data20[9][0],data20[10][0],data20[11][0],data20[12][0],data20[13][0],data20[14][0],data20[15][0]};


  int trimbits4[] = {data25[0][0],data25[1][0], data25[2][0],data25[3][0], data25[4][0],data25[5][0], data25[6][0],data25[7][0],data25[8][0],data25[9][0],data25[10][0],data25[11][0],data25[12][0],data25[13][0],data25[14][0],data25[15][0]};

  int trimbits5[] = {data30[0][0],data30[1][0], data30[2][0],data30[3][0], data30[4][0],data30[5][0], data30[6][0],data30[7][0],data30[8][0],data30[9][0],data30[10][0],data30[11][0],data30[12][0],data30[13][0],data30[14][0],data30[15][0]};

  int trimbits6[] = {data35[0][0],data35[1][0], data35[2][0],data35[3][0], data35[4][0],data35[5][0], data35[6][0],data35[7][0],data35[8][0],data35[9][0],data35[10][0],data35[11][0],data35[12][0],data35[13][0],data35[14][0],data35[15][0]};

  int trimbits7[] = {data40[0][0],data40[1][0], data40[2][0],data40[3][0], data40[4][0],data40[5][0], data40[6][0],data40[7][0],data40[8][0],data40[9][0],data40[10][0],data40[11][0],data40[12][0],data40[13][0],data40[14][0],data40[15][0]};

  int trimbits8[] = {data45[0][0],data45[1][0], data45[2][0],data45[3][0], data45[4][0],data45[5][0], data45[6][0],data45[7][0],data45[8][0],data45[9][0],data45[10][0],data45[11][0],data45[12][0],data45[13][0],data45[14][0],data45[15][0]};

  int trimbits9[] = {data55[0][0],data55[1][0], data55[2][0],data55[3][0], data55[4][0],data55[5][0], data55[6][0],data55[7][0],data55[8][0],data55[9][0],data55[10][0],data55[11][0],data55[12][0],data55[13][0],data55[14][0],data55[15][0]};

  int trimbits10[] = {data60[0][0],data60[1][0], data60[2][0],data60[3][0], data60[4][0],data60[5][0], data60[6][0],data60[7][0],data60[8][0],data60[9][0],data60[10][0],data60[11][0],data60[12][0],data60[13][0],data60[14][0],data60[15][0]};


  //definins a graph for pixel xx
  TGraph *pixel1 = new TGraph(16,trimVals,trimbits1);
  TString pix1 = Form("500",1);
  
  pixel1->SetLineColor(1);
  pixel1->SetMarkerColor(1);
  pixel1->SetMarkerStyle(8);
  pixel1->SetMarkerSize(0.5);
  leg1->AddEntry(pixel1,pix1,"lp");


  //defining a graph for pixel 1500
  TGraph *pixel2 = new TGraph(16,trimVals,trimbits2);
  TString pix2 = Form("1000",2);

  pixel2->SetLineColor(2);
  pixel2->SetMarkerColor(2);
  pixel2->SetMarkerStyle(8);
  pixel2->SetMarkerSize(0.5);
  leg1->AddEntry(pixel2,pix2,"lp");

  TGraph *pixel3 = new TGraph(16,trimVals,trimbits3);
  TString pix3 = Form("1500",3);
  
  pixel3->SetLineColor(3);
  pixel3->SetMarkerColor(2);
  pixel3->SetMarkerStyle(8);
  pixel3->SetMarkerSize(0.5);
  leg1->AddEntry(pixel3,pix3,"lp");
  
  TGraph *pixel4 = new TGraph(16,trimVals,trimbits4);
  TString pix4 = Form("2000",4);
  
  pixel4->SetLineColor(4);
  pixel4->SetMarkerColor(4);
  pixel4->SetMarkerStyle(8);
  pixel4->SetMarkerSize(0.5);  
  leg1->AddEntry(pixel4,pix4,"lp");

  TGraph *pixel5 = new TGraph(16,trimVals,trimbits5);
  TString pix5 = Form("2500",5);
  
  pixel5->SetLineColor(5);
  pixel5->SetMarkerColor(5);
  pixel5->SetMarkerStyle(8);
  pixel5->SetMarkerSize(0.5);  
  leg1->AddEntry(pixel5,pix5,"lp");
  
  TGraph *pixel6 = new TGraph(16,trimVals,trimbits6);
  TString pix6 = Form("3000",6);
  
  pixel6->SetLineColor(6);
  pixel6->SetMarkerColor(6);
  pixel6->SetMarkerStyle(8);
  pixel6->SetMarkerSize(0.5);  
  leg1->AddEntry(pixel6,pix6,"lp");
  
  TGraph *pixel7 = new TGraph(16,trimVals,trimbits7);
  TString pix7 = Form("3500",7);
  
  pixel7->SetLineColor(7);
  pixel7->SetMarkerColor(7);
  pixel7->SetMarkerStyle(8);
  pixel7->SetMarkerSize(0.5);  
  leg1->AddEntry(pixel7,pix7,"lp");
  
  
  TGraph *pixel8 = new TGraph(16,trimVals,trimbits8);
  TString pix8 = Form("4000",8);
  
  pixel8->SetLineColor(8);
  pixel8->SetMarkerColor(8);
  pixel8->SetMarkerStyle(8);
  pixel8->SetMarkerSize(0.5);  
  leg1->AddEntry(pixel8,pix8,"lp");

  TGraph *pixel9 = new TGraph(16,trimVals,trimbits1);
  TString pix9 = Form("500",9);
  
  pixel9->SetLineColor(9);
  pixel9->SetMarkerColor(9);
  pixel9->SetMarkerStyle(8);
  pixel9->SetMarkerSize(0.5);
  leg1->AddEntry(pixel9,pix9,"lp");

  TGraph *pixel10 = new TGraph(16,trimVals,trimbits1);
  TString pix10 = Form("500",10);
  
  pixel10->SetLineColor(10);
  pixel10->SetMarkerColor(10);
  pixel10->SetMarkerStyle(8);
  pixel9->SetMarkerSize(0.5);
  leg1->AddEntry(pixel10,pix10,"lp");



  TMultiGraph *mg = new TMultiGraph();

  mg->Add(pixel1);
  mg->Add(pixel2);  
  mg->Add(pixel3);
  mg->Add(pixel4);
  mg->Add(pixel5);
  mg->Add(pixel6);
  mg->Add(pixel7);
  mg->Add(pixel8);
  mg->Add(pixel9);
  mg->Add(pixel10);
  //mg->SetMarkerStyle();
  mg->Draw("ALP");  
  leg1->Draw();
  mg->SetTitle("Module1 VCal vs Vtrim ");
  mg->GetXaxis()->SetTitle("Vcal [DAC]");
  mg->GetXaxis()->SetTitleOffset(1.1);
  mg->GetYaxis()->SetTitle("Vtrim [DAC]");
  mg->GetYaxis()->SetTitleOffset(1.1);


  // pixel1->Draw("ALP");

}

